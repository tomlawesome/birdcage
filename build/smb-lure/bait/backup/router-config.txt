# Border router - exported settings summary
# Exported by the weekly configuration backup task.
# Addresses below are documentation ranges (RFC 5737), not live.

system
  name            br-01
  timezone        Europe/London
  ntp             192.0.2.10, 192.0.2.11

interfaces
  wan             pppoe, mtu 1492
  lan             192.0.2.1/24
  guest-wlan      198.51.100.1/24, isolated

routing
  default         via wan
  static          203.0.113.0/24 via 192.0.2.254

firewall
  wan -> lan      drop, established/related allow
  guest -> lan    drop
  lan -> wan      allow

services
  dhcp            192.0.2.100 - 192.0.2.200, lease 12h
  dns forwarders  192.0.2.10, 192.0.2.11
  remote admin    disabled on wan

# No secrets are exported by this task. Accounts, keys and pre-shared
# keys are held in the password manager and are not part of this file.
