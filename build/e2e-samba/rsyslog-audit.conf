# Routes vfs_full_audit's syslog(3) calls (facility local7, see smb.conf)
# into the file OpenCanary watches. Nothing else in this image uses
# local7, so this rule is exact rather than a catch-all.
local7.*    /samba-audit/samba-audit.log
